# allocates graphics buffers through the legacy ION interface (/dev/ion), rw_file_perms includes open/read/write/ioctl/map which ION needs.
allow hal_graphics_allocator_default ion_device:chr_file rw_file_perms;